{
  "name": "@whatnext/core",
  "version": "1.0.0",
  "private": true,
  "description": "Storage-agnostic recommendation engine: interest modeling, providers, ranking, explanations, timeline, digest.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "watch": "tsc -p tsconfig.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.4"
  }
}
